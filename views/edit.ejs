<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Edit your chat</title>
</head>
<body>
    <p>Edit your chat: <%=  chat._id %></p>
    <p>
        This chat was sent from <b><%= chat.from %></b> to <br><%= chat.to %></b>
    </p>

    <p> previous message was: <b> <%= chat.msg %>  </b> </p>

    <form method="POST" action="/chats/<%= chat.id %>?_method=PUT"> 
        <!-- changeing post to put request -->
        <!-- using method-overwrite npm packge -->
        <textarea  placeholder="write your msg" name="msg" rows="6" cols="50"> <%= chat.msg %>  </textarea>
        <br>
        <button>Update</button>
    </form>
</body>
</html>